<%= form_for([@lunch_special, @lunch_special.pictures.create]) do |f| %>
  <% if @lunch_special.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lunch_special.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
      <% @picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
    <% end %>

    <%= f.file_field :image %>
      <%= f.submit "Submit", :disable_with => 'Submiting...' %>

      
<% end %>




